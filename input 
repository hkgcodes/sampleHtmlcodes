<p:dataTable var="item" value="#{yourBean.yourList}">
    <p:column headerText="Column Header">
        <div style="display: flex; align-items: center;">
            <h:inputText value="#{item.inputValue}" size="10" />
            <h:commandButton value="Button 1" action="#{yourBean.button1Action}" />
            <h:commandButton value="Button 2" action="#{yourBean.button2Action}" />
        </div>
    </p:column>
    <!-- Other columns and configurations go here -->
</p:dataTable>